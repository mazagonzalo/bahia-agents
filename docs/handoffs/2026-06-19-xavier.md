# Handoff — Xavier · 2026-06-19

## Qué hice
**FASE 0 del re-plataformado: dashboard unificado por agente.** Convertí el dashboard monolítico
(`app/dashboard/page.tsx`, 1 archivo de ~766 líneas que solo mostraba tendencias + crítico, tema claro
con Cormorant Garamond) en un **shell oscuro multi-ruta** donde **cada uno de los 9 agentes tiene su
propia página**. PR a `dev`: `feat/dashboard-ui`.

- **Shell compartido**: `app/dashboard/layout.tsx` (sidebar por registro + header por ruta + UserButton).
- **Registro único**: `app/dashboard/_lib/agents.ts` (9 agentes + leads, agrupados: Crecimiento / Ventas & CRM / Inteligencia / Operación).
- **Primitivos compartidos**: `app/dashboard/_components/` (ui.tsx con Ring/ScoreBar/StatCard/Card/Badge/Skeleton…, icons, Sidebar, DashboardHeader, ChatPanel, TriggerPanel).
- **Una ruta por agente** en `app/dashboard/<slug>/page.tsx`:
  - **Tendencias** movido a `/dashboard/tendencias` (preserva la generación optimista de 290s + fallback a /report).
  - **Crítico** separado a `/dashboard/critico`.
  - **Data** (GET en mount): meta-ads, seguimiento, reputacion. **Trigger** (POST): contenido, eventos. **Chat** (POST→reply): ventas, secretaria. **CRM**: leads (preview vía seguimiento GET; el CRM gobernado llega en Fase 4).
  - `/dashboard` = overview con tarjetas de agentes.
- **Diseño**: design-system OSCURO (gold #D4A853 + Sora/Inter), ya consolidado en `app/globals.css`
  (clases `.dashboard-shell/-sidebar/-content`, `.card`, `.metric-card`, `.badge-*`, `.data-table`, `.btn-*`, `.grid-*`).
- **Pulido premium**: skeletons de carga, estados vacíos/error on-brand, formato de números es-MX, responsive (<900px).

## Por qué
El `PENDIENTES.md` pedía "unificar todos los agentes en un solo dashboard con navegación por secciones"
y rediseñar con branding. Esto lo cumple y además deja la base para enchufar el harness gobernado:
cada página de agente es el lugar natural para, en la Fase 3, mostrar sus `AgentRunLog`/`AgentApproval`.

## Estado actual (qué corre / qué falla)
- ✅ **CI verde local**: `tsc --noEmit` 0 · `eslint app/dashboard` 0 · `next build` verde (16 rutas, las 11 del dashboard prerenderean).
- ✅ Los 9 agentes de Gonzalo **siguen intactos** — solo agregué UI que consume sus rutas existentes (mismos endpoints/payloads). No toqué `app/api/*` ni `lib/*`.
- ⚠️ **No verificado visualmente autenticado**: `/dashboard` está detrás de Clerk; sin `.env.local` local no puedo renderizarlo logueado. Verificar en el **preview de Vercel** de esta rama (logueándose con Clerk).
- Backup de seguridad: rama `backup/dashboard-ui-20260619`.

## Siguiente paso
- **Mergear `feat/dashboard-ui` → `dev`** (este PR). Tu PR #11 (dev→main) lo recogerá solo cuando hagas el release.
- **FASE 1 (Prisma)**: sigue bloqueada por **`DATABASE_URL`/`DIRECT_URL`** de Supabase. Decisión tomada:
  Prisma sobre el **mismo Postgres de Supabase** (gobierno: migraciones, `AgentRunLog`, enums, RLS deny-by-default;
  pgvector como `Unsupported("vector(1536)")` + escape hatch supabase-js). En cuanto haya tokens: `prisma db pull`
  de la DB viva → baseline → migración de seguridad RLS.

## Archivos tocados
- Nuevos: `app/dashboard/_lib/agents.ts`, `app/dashboard/_components/*` (6), `app/dashboard/layout.tsx`, `app/dashboard/{tendencias,critico,contenido,eventos,meta-ads,ventas,seguimiento,reputacion,secretaria,leads}/page.tsx`.
- Modificados: `app/dashboard/page.tsx` (monolito → overview), `app/globals.css` (design-system oscuro), `app/layout.tsx` (fuentes Inter/Sora).
- NO toqué: `app/api/*`, `lib/*`, `proxy.ts`, `supabase/schema.sql`.

## Bloqueos / dudas para Gonzalo
- **Reemplacé tu dashboard monolítico** (el de PR #11). Tu trabajo de agentes queda igual; solo cambia la capa de UI. Si tenías pendiente algo más en `app/dashboard/page.tsx`, avísame y lo integro.
- Necesitamos **`DATABASE_URL`/`DIRECT_URL`** de Supabase (pooled 6543 + directo 5432) para arrancar Prisma — ¿lo tienes a la mano o lo saco con `vercel env pull`?
- Role guard del dashboard (OWNER/ADMIN) está como TODO (Fase 5) detrás de `DASHBOARD_REQUIRE_ROLE` para no bloquearnos en desarrollo; hoy cualquier usuario logueado entra (igual que antes).
