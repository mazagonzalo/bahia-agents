# Handoff — 2026-06-15 — Gonzalo

**Qué hice:**
- Mergeé el PR #1 de Xavier (saneamiento build/seguridad/CI) a `main` vía merge local + push con scope `workflow`.
- Fusioné el sitio HTML del club en `public/`: `bahia-sitio.html`, `assets/`, `css/`, `js/`, y 4 páginas de documentos (presentación, comparativos, guía).
- `app/page.tsx` → redirige a `/bahia-sitio.html`. `next.config.ts` → rewrites limpios para `/presentacion`, `/comparacion`, `/guia`.
- Actualizado metadata del layout (`Bahía · Social Sports Club`).
- Creada rama `dev` y subida a GitHub — de ahora en adelante todo va por rama → PR a `dev`.
- Actualizado bloque Gonzalo en `docs/STATUS.md`.

**Por qué:**
- El cliente quería un solo proyecto en Vercel con el sitio web + los agentes conviviendo.
- El PR de Xavier no podía mergearse vía `gh` sin el scope `workflow` — se resolvió con `gh auth refresh`.

**Estado actual (qué corre / qué falla):**
- `main` limpio y en verde en GitHub. Vercel deployará automáticamente desde `main`.
- Los 4 checks rojos del PR eran protección de fork de Vercel — no errores de código. Tras el merge a `main` desaparecen.
- El sitio HTML vive en `public/` y se sirve desde `/bahia-sitio.html`.

**Siguiente paso:**
- Agregar `CRON_SECRET` en Vercel (env vars del proyecto) — los crons de Xavier lo requieren.
- Dar acceso Write a `xaviersalass-web` en GitHub → Settings → Collaborators.
- Leer los 4 docs de `docs/estrategia/` de Xavier para entender el Motor Day Pass → Inscripción.
- Limpiar los 3 proyectos Vercel extra (`bahia_agents`, `bahia_agents_claude`, `bahia-agents-37jp`) — solo debe quedar uno.

**Archivos tocados:**
- `app/page.tsx`, `app/layout.tsx`, `next.config.ts` (redirect + rewrites).
- `public/` (sitio HTML + assets completos).
- `docs/STATUS.md` (bloque Gonzalo).
- `docs/handoffs/2026-06-15-gonzalo.md` (este archivo).

**Bloqueos / dudas para el otro:**
- El sitio HTML usa rutas relativas (`assets/`, `css/`, `js/`) — funcionan bien desde `public/` en Next.js.
- Hay 4 proyectos Vercel conectados al mismo repo — hay que desconectar 3 desde el dashboard de Vercel.
- `CRON_SECRET` no está en Vercel aún — los crons van a responder 503 hasta que se agregue.
