# Handoff — 2026-06-22 — Gonzalo

**Qué hice:**
- **Rediseñé el agente de contenido** con 2 funciones y lo **mergeé a producción** (PR #29 → `dev`, PR #30 → `main`):
  1. **Sugerencias de contenido (apoyo de ideas):** escribes un tema (ej. "atardecer con pickleball") → genera 3-4 **guías detalladas** (formato reel/historia/post, música, duración, gancho, cómo grabarlo) + **score** c/u según tendencias. NO escribe drafts; es apoyo para el contenido normal que hace Bahía.
  2. **Carrusel promocional (ciclo de pauta de 14 días):** genera **3 variantes con ángulos distintos** → el admin **aprueba 1** → el agente **deriva 2 más del mismo estilo** (cola de rotación, `status: en_cola`) + brief de video complementario real.
- **Cron `promo-cycle`** (días 1 y 15 de cada mes ≈ cada 14 días): lee el reporte de tendencias y dispara la generación. **Quité el auto-dispatch DIARIO** de tendencias→contenido (era costoso).
- Slides **flexibles (2-8)**, **foto específica por slide** (describe la toma real, no "foto de instalaciones"), **pide solo las fotos que faltan**. **Sin IA/muapi** (eliminé `buildCarouselImagePrompt`/`buildVideoPrompt`).

**Por qué:**
- El agente "no cumplía su función" (solo generaba copy). Ahora es apoyo de ideas + motor de carruseles promocionales pauteables con rotación.
- Mergeé a producción para **activar el cron automático** y dejar la vista accesible desde el **login real** (sin el muro de SSO de los previews).

**Estado actual (qué corre / qué falla):**
- ✅ En **producción** (`main` @ `244fd47`): agente de contenido con sus 2 funciones + cron `promo-cycle` registrado en `vercel.json`. CI verde (tsc/lint/build/E2E) en #29 y #30.
- ✅ **Tendencias ↔ Contenido comparten datos** vía `agent_memory` (tendencias escribe el `briefing`, contenido lo lee en sugerencias y en el cron).
- ⚠️ `club_assets` está **vacío** → siempre pide todas las fotos (faltan fotos reales del club).
- ⚠️ **El ciclo NO está cerrado todavía:** aprobar una variante **no lanza la pauta** en meta-ads, y el **crítico no rota** la cola (la cola se crea, pero nada la activa cuando baja la curva).

**Siguiente paso:**
- Subir **fotos reales** del club a `club_assets` (onboarding) — desbloquea fotos específicas + Canva.
- **Cerrar el loop:** creativo aprobado → meta-ads (lanzar campaña) + crítico → contenido (activar la siguiente variante de la cola al bajar la curva de interacción).
- **Canva** para ensamblar el carrusel final (necesita template Bahía + las fotos).

**Archivos tocados:**
- `app/api/agents/contenido/route.ts` — rediseño completo (sugerencias + ciclo de pauta + variantes + foto por slide)
- `app/dashboard/contenido/page.tsx` — vista nueva (sugerencias expandibles + generar/aprobar/rotación)
- `app/api/cron/promo-cycle/route.ts` — nuevo cron biweekly
- `app/api/agents/tendencias/route.ts` — quita el auto-dispatch diario a contenido + limpia `isDev` huérfano
- `vercel.json` — registra el cron `promo-cycle`
- `PENDIENTES.md` — nota del bug del harness para ti (Xavier) · `.gitignore` — resuelto (+ `/.clerk/`)

**Bloqueos / dudas para Xavier:**
- 🔴 **Bug del harness en producción (te lo dejé en `PENDIENTES.md`):** `GET /api/harness/runs` y `/api/harness/approvals` devuelven **500 `Clerk: auth() was called...`**. Hipótesis: el matcher de `proxy.ts` solo cubre `/dashboard` y `/admin`, así que `auth()` dentro de `/api/harness/*` corre **fuera del contexto de clerkMiddleware**. Fix probable: incluir `/api/harness/(.*)` en el matcher. Es de tu dominio (el gobierno).
- Para **cerrar el ciclo de pauta** necesito de tu lado: cómo lanzar una campaña en **meta-ads** desde un creativo aprobado, y qué señal expone el **crítico** cuando "baja la curva" para disparar la rotación. ¿Lo coordinamos?
- ⚠️ El **reporte de tendencias a veces sale truncado** (JSON incompleto → solo `trends`/`googleTrends`, sin estrategia/estacionalidad). Contenido degrada bien, pero conviene blindar la generación de tendencias (retry/defaults) para que el contexto llegue completo.
