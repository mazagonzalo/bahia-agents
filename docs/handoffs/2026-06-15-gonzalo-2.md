# Handoff — 2026-06-15 (sesión 2) — Gonzalo

> Segundo handoff del día. El primero (`2026-06-15-gonzalo.md`) cubrió el setup inicial.
> Esta sesión fue de infraestructura CI/CD + review/merge de los PRs de Xavier.

**Qué hice:**
- **E2E Playwright (PR #2):** creé `e2e/sitio.spec.ts` (5 tests rutas públicas), `playwright.config.ts`,
  job `e2e` en `.github/workflows/ci.yml`, script `test:e2e` en `package.json`. Pasaron 5/5 localmente y en CI.
- **Vercel Pro:** diagnostiqué por qué fallaba el deploy en preview — el plan Hobby no permite crons
  en ningún ambiente (preview ni production) cuando `vercel.json` tiene el array `crons`. El usuario
  subió al plan Pro. Re-agregué los 3 crons: `daily-trends` (14h), `lead-followup` (c/4h), `weekly-report` (lunes 15h).
- **CRON_SECRET:** generé con `openssl rand -hex 32` y lo agregué a Vercel en Production + Preview
  via `vercel env add`. Hice un empty commit a `main` para forzar redeploy y activarlo.
- **Review + merge PR #3 de Xavier (Clerk auth):** Clerk protege solo `/dashboard` y `/admin`.
  Tuve que resolver dos fallos de CI en secuencia:
  - `Missing publishableKey` → faltaba `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` en GitHub Secrets.
  - `Handshake token verification failed` → Clerk valida `CLERK_SECRET_KEY` incluso en rutas públicas;
    faltaba en GitHub Secrets. El usuario agregó ambas → re-run pasó 5/5 → mergeé PR #3 a dev.
- **Merge dev → main** (commit `b7b093d`): producción queda con Clerk + E2E + CI verde + crons + CRON_SECRET.

**Por qué:**
- Xavier necesitaba CI verde para poder mergear su Clerk PR. Los tests fallaban porque la middleware
  de Clerk intercepta todas las rutas (incluso las públicas) y requiere las keys en runtime.

**Estado actual (qué corre / qué falla):**
- `main` en producción: verde en todos los checks (build + typecheck + lint + E2E + Vercel).
- Vercel Pro activo, proyecto único: `bahia-agents-37jp`.
- 3 crons activos. CRON_SECRET configurado.
- Clerk activo: `/dashboard` y `/admin` requieren login. Todo lo demás es público.
- GitHub Secrets configurados: `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` + `CLERK_SECRET_KEY`.
- Nada roto.

**También hice (al final de la sesión):**
- **`DATABASE_URL` y `DIRECT_URL` en Vercel** ✅ — Gonzalo los agregó desde Supabase Settings → Database
  → Connection string (Shared Pooler). `DATABASE_URL` usa puerto 6543 (pooler), `DIRECT_URL` usa 5432
  (directo, para migraciones Prisma). Deploy hecho. **Fase 2 de Xavier desbloqueada.**
- El proyecto Supabase (`bahia-agents`, proyecto ID `wrlngeqgvhxsozfvrdgh`, región us-east-1) ya tenía
  las tablas creadas: `leads`, `conversations`, `agent_memory`, `creatives`, `trends`.

**Siguiente paso:**
- **Xavier puede arrancar Fase 2 (Prisma)** — `DATABASE_URL` y `DIRECT_URL` ya están en Vercel.
  Correr `vercel env pull` para jalarlo local y arrancar con `prisma generate` + `prisma migrate`.
- Configurar webhook de **WhatsApp Business API** en Meta for Developers (URL del webhook, token de verificación).
- Agregar `CLERK_SECRET_KEY` al `.env.local` local para que `npm run dev` funcione con auth.

**Archivos tocados:**
- `e2e/sitio.spec.ts` — nuevo
- `playwright.config.ts` — nuevo
- `.github/workflows/ci.yml` — job E2E + env vars Clerk
- `vercel.json` — 3 crons (temporalmente vacío, luego restaurado)
- `package.json` — script `test:e2e` + devDep `@playwright/test`
- `.gitignore` — `test-results/` + `playwright-report/`

**Bloqueos / dudas para el otro (Xavier):**
- ✅ `DATABASE_URL` / `DIRECT_URL` — **ya están en Vercel**. Fase 2 desbloqueada.
- ✅ `CRON_SECRET` — ya está en Vercel.
- ✅ Keys de Clerk — ya están en Vercel y en GitHub Secrets.
- Sin bloqueos conocidos de parte de Gonzalo.
