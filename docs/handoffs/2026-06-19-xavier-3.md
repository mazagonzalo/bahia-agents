# Handoff — Xavier · 2026-06-19 (sesión 3, parte 3) — RE-PLATAFORMADO COMPLETO

> **TL;DR para Gonzalo:** las 6 fases del re-plataformado (commerce-os → bahia) están **hechas y
> mergeadas a `dev`** (PRs #12–#26). Los 9 agentes ahora corren sobre **Prisma + harness gobernado**,
> sin perder su comportamiento. **Falta SOLO 1 cosa tuya OBLIGATORIA para que el deploy corra E2E:
> poner `DATABASE_URL`/`DIRECT_URL` (pooled) en Vercel + redeploy.** Detalle abajo.

---

## ✅ Qué se hizo (las 6 fases)

| Fase | Qué | PRs | Verificación |
|---|---|---|---|
| **0 · Dashboard** | Monolito (1 page) → shell oscuro multi-ruta, **1 página por agente** + `/dashboard/harness` (Gobierno) + login en el landing | #12, #13(UI), #19 | tsc/lint/build verde, CI verde |
| **1 · Prisma** | Prisma sobre el MISMO Postgres de Supabase. `db pull` de la DB viva, migraciones `0_init` + `1_governance_harness` (6 tablas Agent* + enums + RLS) **APLICADAS a prod** | #13 | E2E: gobierno creado, tablas legacy intactas (`trends=52`) |
| **2 · Harness** | Portado de commerce-os: `runAgent`/context-store/prompt-registry/evals. 9 prompts v1 sembrados | #13 | **`eval:agents` 18/18 fixtures, juez 4.5–5.0/5** |
| **3 · Los 9 agentes** | critico, reputacion, seguimiento, contenido, eventos, meta-ads, ventas, secretaria, tendencias → **gobernados** (lecturas/escrituras a Prisma; `sendText` fail-soft; evaluadores vía `runAgent({skipApproval})`) | #14–#23 | **Cada uno verificado E2E real** (curl + AgentRunLog/datos antes/después) |
| **4 · CRM** | Webhook WhatsApp (ingesta de leads) → Prisma. `/dashboard/leads` (lista) + `/dashboard/leads/[id]` (detalle + hilo de conversación) | #25 | tsc/lint/build verde |
| **5 · Hardening** | Role guard env-gated (`DASHBOARD_REQUIRE_ROLE`) en el dashboard + migración `2_rls_legacy_hardening` (RLS deny-by-default en tablas legacy) | #26 | tsc/build verde; RLS **autorada, NO aplicada** |

**Decisión de datos:** Prisma sobre el mismo Supabase (no segunda DB). Gobierno = migraciones en git,
`AgentRunLog` (costo/tokens por corrida), enums, `AgentApproval` (gate), `AgentContext` (memoria),
`AuditLog`. pgvector se queda como escape hatch en supabase-js. **Ningún agente se rompió** — cada uno
conserva su contrato externo (lo que consume el dashboard/webhook); el gobierno entró por debajo.

**Verificado en LOCALHOST que TODO el código jala** (todos los reads del dashboard → HTTP 200 con la
`DATABASE_URL` pooled). El código es correcto; lo único que falta es el entorno de Vercel.

---

## 🔴 GONZALO — LO QUE DEBES HACER OBLIGATORIAMENTE PARA QUE CORRA E2E

### 1. (CRÍTICO) `DATABASE_URL` + `DIRECT_URL` en Vercel + redeploy
**Sin esto, el dashboard desplegado se ve roto** (critico "sin evaluación", harness "sin datos",
leads/seguimiento no cargan) — porque los 9 agentes ahora leen de Prisma y Vercel no puede conectar.
- Vercel → proyecto `bahia-agents-37jp` (team `mazaprojects`) → Settings → Environment Variables → **los 3 entornos**:
```
DATABASE_URL = postgresql://postgres.wrlngeqgvhxsozfvrdgh:<PASSWORD_SUPABASE>@aws-1-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true
DIRECT_URL   = postgresql://postgres.wrlngeqgvhxsozfvrdgh:<PASSWORD_SUPABASE>@aws-1-us-east-1.pooler.supabase.com:5432/postgres
```
- ⚠️ Usa el **POOLER** (`...pooler.supabase.com`, IPv4). **NO** la directa `db.xxx.supabase.co` (IPv6, Vercel no la alcanza).
- `<PASSWORD_SUPABASE>` = la contraseña de la DB (la tiene Xavier; o sácala de Supabase → Database).
- **Redeploy** tras guardar. → Todo el dashboard funciona de golpe.

### 2. (Recomendado) Aplicar la migración RLS de seguridad
La migración `2_rls_legacy_hardening` (RLS deny-by-default en las tablas legacy) está **autorada pero no
aplicada** (un guardrail me bloqueó correrla sobre tus tablas). Es segura (el owner/service bypassan RLS).
Para aplicarla:
```
npx dotenv -e .env.local -- npx prisma migrate deploy
```
(El `_prisma_migrations` ya tiene `0_init` + `1_governance_harness` aplicadas; esto solo aplica la #2.)

### 3. (Opcional, limpieza) 2 artefactos de prueba en la DB
Mis tests E2E dejaron 1 evento ("Torneo de Pádel – Mixto y Avanzado") y 1 creative de prueba (carrusel
sobre "clases de pádel para principiantes"). Inocuos, pero si quieres limpiarlos, bórralos por ID.

---

## Estado / notas
- **`contenido/onboarding`** (sub-ruta) es lo único que sigue en supabase-js (menor; migrable después).
- **Modelos de identidad** (User/CustomerProfile de commerce-os) se difirieron a la **fase Trainingym**
  (es donde aterrizan los socios reales).
- Para correr el dashboard **en localhost**: `.env.local` necesita las **Clerk keys reales**
  (`NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` + `CLERK_SECRET_KEY`, las de Vercel) — hoy tiene placeholders.
  La `DATABASE_URL` pooled ya está en el `.env.local` de Xavier y funciona.

## Siguiente fase: Trainingym (cuando haya acceso del dueño)
La base ya está perfecta (DB gobernada + harness). Plan: **ingesta** (cron → tablas Prisma
`Member`/`Attendance`/`ClassSession`) + **agente BI** (portar `bi-analyst` de commerce-os) +
**`/dashboard/analytics`**. **Se necesita del dueño:** ¿Trainingym tiene API? credenciales, o un export
de ejemplo para definir la ingesta.

## PRs de la sesión (todos merged a `dev`)
#12 dashboard · #13 Prisma+harness+Gobierno · #14–#23 los 9 agentes · #19 login landing · #24 recordatorio
Vercel · #25 CRM · #26 hardening.
