# Handoff — Xavier · 2026-06-19 (sesión 3, parte 2)

## Qué hice
**Fases 1, 2 y 3-lite del re-plataformado** (PR #13 `feat/prisma-harness`→`dev`). **Todo ADITIVO — no toqué tus 9 rutas de agentes.**

- **Fase 1 · Prisma (gobierno de datos):** `prisma db pull` de la DB viva (capturó las 7 tablas reales; **`reviews` NO existe**, el código la lee con fallo silencioso). Migraciones `0_init` (baseline, marcada applied) + `1_governance_harness` (6 tablas Agent* + 5 enums + RLS deny-by-default en las nuevas), **aplicadas a prod con `migrate deploy`**. Verificado aditivo (0 DROP/ALTER) y que tus tablas siguen intactas.
- **Fase 2 · Harness:** porté el harness gobernado de commerce-os (`runAgent`, context-store, prompt-registry, coordinator, evals). 9 prompts/wrappers/fixtures (reexpresé tu intención de dominio al contrato JSON estricto del harness). `eval:agents` corre **18/18 fixtures, juez 4.5–5.0/5**.
- **Fase 3-lite · UI de Gobierno:** `/dashboard/harness` (ledger de costo/tokens + cola de aprobaciones) + `app/api/harness/*`.

## Por qué
Es el "enbone" con commerce-os: tus agentes ganan trazabilidad (cada corrida → `AgentRunLog` con costo), memoria gobernada (`AgentContext`), prompts versionados (`AgentPromptVersion`) y gate de aprobación (`AgentApproval`) — sin perder pgvector ni romper nada.

## Estado actual (qué corre / qué falla)
- ✅ DB de prod: 6 tablas de gobierno creadas (con RLS), 9 prompts v1 sembrados. Tus tablas intactas (`trends=52`, `creatives=1`).
- ✅ `tsc` 0 · `eslint` 0 errores · `next build` verde (19 rutas) · `eval:agents` 18/18.
- ⚠️ **El gobierno corre en ISOLATION** (evals). Tus agentes vivos **todavía NO** lo usan — eso es la Fase 3 (rewire), a propósito 1-agente-por-PR para no romperlos.
- ⚠️ `/dashboard/harness` necesita `DATABASE_URL`/`DIRECT_URL` en el entorno de **Vercel** para leer en el preview/prod (si no, muestra estado vacío). Confirmar que estén ahí.

## Siguiente paso
- **Fase 3:** rewire de cada ruta de agente para que pase por `runAgent()` + escriba a Prisma, **1 agente por PR**, empezando por los de bajo riesgo (critico/reputacion/seguimiento). Coordino contigo antes de tocar cada uno.
- **Fase 4:** modelos `Lead`/`Conversation` gobernados + identidad Clerk + UI CRM.
- **Fase 5:** RLS deny-by-default en las tablas legacy (hoy `USING(true)`) + auth por rol en el dashboard.

## Archivos tocados
Nuevos: `prisma/schema.prisma` (reconciliado), `prisma/migrations/*`, `lib/db.ts`, `lib/agents/*` (harness completo), `lib/types/agent.ts`, `prisma/seed-harness.ts`, `app/api/harness/*`, `app/dashboard/harness/page.tsx`, `docs/PRISMA_SETUP.md`. Modificados: `package.json` (deps+scripts), `.env.example`, `app/dashboard/_lib/agents.ts` (nav Gobierno).

## Bloqueos / dudas para Gonzalo
- ¿`DATABASE_URL`/`DIRECT_URL` ya están en Vercel? (Para que el dashboard de Gobierno lea en prod.)
- Para la Fase 3 (rewire de tus agentes) coordinamos: empiezo por uno, lo pruebo, y revisas antes de seguir.
- Release `dev`→`main` (tu PR #11) cuando quieras: ya incluye el dashboard nuevo + esto.
